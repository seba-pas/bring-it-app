const axios= require ("axios");
const { Router } = require('express');
const {Purchase, Product, User} = require('../../db');


const deletPedido = async ()=>{
    
};

module.exports={getDbPedidos, };